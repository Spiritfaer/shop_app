import 'package:flutter/material.dart';

class ProtuctsOverviewScreen extends StatelessWidget {
  const ProtuctsOverviewScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('some protucts'),
    );
  }
}
